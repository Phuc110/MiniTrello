================================================================================
 MINI TRELLO — CSE 456 Final Project
 SESSION NOTES — 2026-08-22 (Asia/Ho_Chi_Minh)
================================================================================

File này ghi lại toàn bộ công việc của session hôm nay: ĐÃ LÀM / ĐANG LÀM /
SẼ LÀM. Các note cũ: PROGRESS-NOTES.txt (CI fixes 2026-08-13..17),
REFACTOR-NOTES.txt (refactor bỏ tầng Project, 2026-08-20).

--------------------------------------------------------------------------------
A. ĐÃ LÀM (hoàn thành + đã verify)
--------------------------------------------------------------------------------

A1. KIỂM TRA TOÀN BỘ TRẠNG THÁI SAU REFACTOR
    - Backend unit/controller tests PASS, frontend lint + 16 test + build PASS.
    - Docker Desktop khởi động, rebuild compose stack, smoke test API OK
      (login pm@test.com/DemoPass1, board auto 3 lists, my-tasks,
      DELETE /board-lists/{id}, members).

A2. FIX BUG "XÓA LIST NHƯNG CỘT VẪN KHÔNG MẤT" (root cause BACKEND)
    - Nguyên nhân thật: @SQLRestriction("deleted_at IS NULL") đặt trên
      @MappedSuperclass bị Hibernate BỎ QUA (không kế thừa xuống entity con)
      => mọi SELECT làm lộ row soft-delete (chứng minh bằng SQL log:
      "where bl1_0.board_id=? " thiếu điều kiện deleted_at).
    - Fix: thêm @SQLRestriction("deleted_at IS NULL") TRỰC TIẾP lên 5 entity:
      BoardList, Board, Task, Workspace, User (+ cập nhật comment trong
      SoftDeletableEntity để tránh tái phạm).
    - Frontend (KanbanColumn.tsx): onSuccess giờ setQueryData lọc list khỏi
      cache ngay + invalidateQueries.
    - Verify: tái hiện trên stack thật — xóa "Done" => GET còn đúng 2 lists;
      SQL log có "where (bl1_0.deleted_at IS NULL)".

A3. FIX BUG "NÚT DELETE WORKSPACE RUNG + BẤM KHÔNG ĂN"
    - Nguyên nhân: overlay fixed inset-0 z-10 + card hover:-translate-y-0.5
      (transform tạo stacking context nhốt dropdown dưới overlay) => hover
      flicker và click luôn rơi vào overlay.
    - Fix vòng 1 (WorkspaceListPage.tsx): bỏ overlay, dùng document mousedown
      listener; thêm preventDefault/stopPropagation; setQueryData xóa card
      khỏi cache ngay sau khi xóa.
    - Regression đã phát hiện + sửa: menuRef dùng CHUNG cho mọi card chỉ trỏ
      vào card cuối => bấm Delete ở card khác bị nuốt click. Fix: gắn ref có
      điều kiện ref={openMenuId === workspace.id ? menuRef : null}.
    - Fix vòng 2 (theo yêu cầu user): bỏ hẳn menu ⋯ dropdown, thay bằng NÚT
      THÙNG RÁC giống hệt card board (hiện khi hover, đỏ khi hover nút, mở
      thẳng DeleteConfirmModal). Chỉ owner thấy nút (check canDelete từ BE).
      Dọn sạch openMenuId/menuRef/useEffect/MoreVertical.
    - Backend cascade delete workspace verify OK theo design hiện tại:
      WorkspaceService.deleteWorkspace soft-delete thủ công theo thứ tự
      tasks -> lists -> boards -> members -> workspace (project dùng plain FK
      cố ý nên KHÔNG dùng CascadeType.ALL/orphanRemoval).

A4. MỞ RỘNG DỮ LIỆU DEMO (DemoDataSeeder.java)
    - 2 workspaces: "Demo Workspace" (slug demo-workspace),
      "Innovation Lab" (slug innovation-lab). Owner = pm@test.com,
      member@test.com chỉ là member được mời.
    - 3 boards / 17 lists / 32 tasks:
        + Product Roadmap   — 6 lists, 12 tasks
        + Website Redesign  — 5 lists, 10 tasks
        + Mobile App Launch — 6 lists, 10 tasks
    - Đủ priority, tag (mỗi workspace bộ tag riêng), assignee, due date
      (có overdue + due-soon để demo notification scheduler).
    - Seeder vẫn chạy ĐÚNG MỘT LẦN (guard theo slug + app.demo-data.enabled)
      — tài khoản đăng ký mới không bị sinh dữ liệu.
    - DB reset sạch (docker compose down -v && up --build) rồi seed lại.

A5. BOARD VIEW — LOẠT FIX GIAO DIỆN VÒNG 1
    - TaskModal bị ép khung: Modal size xl nới max-w-4xl -> max-w-5xl;
      form đổi sang flex (trái flex-1 min-w-0, phải aside lg:w-72
      flex-shrink-0) — hết cảnh cột phải bị dồn.
    - Popover Filter bị card đè: nguyên nhân là header chứa popover dùng
      backdrop-blur-md tạo stacking context bậc 0, canvas nằm sau DOM nên
      vẽ đè. Fix: thêm relative z-30 cho header bar (BoardPage.tsx).
    - Cột list tràn màn hình: canvas flex-1 thiếu min-h-0 (flex min-height
      mặc định là auto) nên lớn theo nội dung. Fix: min-h-0 +
      overflow-y-hidden cho canvas; giờ cột cao đúng viewport, chỉ vùng
      cards giữa cuộn (overflow-y-auto scrollbar-thin), header/footer đứng yên.

--------------------------------------------------------------------------------
B. ĐÃ LÀM TIẾP (vòng refactor UI thứ 2 — cùng ngày)
--------------------------------------------------------------------------------

B1. REFACTOR TASK DETAIL MODAL HOÀN TẤT (TaskModal.tsx + Modal.tsx)
    - Modal.tsx: thêm size "2xl" = max-w-[800px] (cập nhật cả type union);
      overlay sẵn có fixed inset-0 z-50 flex items-center justify-center.
    - TaskModal dùng size="2xl"; form grid grid-cols-1 md:grid-cols-3 gap-6:
      trái md:col-span-2 (Title / Description textarea resize-y min-h-[110px]
      / Assigned Members + Assign popover), phải md:col-span-1 Task
      Configuration (Priority, Due Date, Tags, Save/Delete/Cancel).
    - Responsive: mobile stack 1 cột như cũ.

B2. GIỚI HẠN CHIỀU CAO CỘT ~4 CARDS (KanbanColumn.tsx)
    - Container cards thêm max-h-[520px] + pr-1.5 (giữ overflow-y-auto
      scrollbar-thin); header/footer vốn nằm ngoài vùng cuộn nên tự ghim.

B3. QUẢN LÝ TAG TRONG TASK MODAL HOÀN TẤT
    - Popover "Add / edit tags": liệt kê tag của workspace (dot màu + tên,
      check đánh dấu đang gắn), click toggle ngay qua taskApi.addTag/
      removeTag (POST/DELETE /api/tasks/{taskId}/tags/{tagId}) — PUT update
      của BE không nhận mảng tags nên áp dụng tức thì thay vì gộp vào Save.
    - Tag đã gắn hiển thị pill màu + nút x tháo nhanh; local state taskTags
      chống props snapshot; invalidate ["tasks", boardListId] sau mỗi lần.
    - Verify API end-to-end: ADD -> [Bug, Feature], REMOVE -> [Feature]. OK.
    - Lưu ý TS: khi thêm size mới cho Modal phải sửa cả type union trong
      interface ModalProps (đã gặp lỗi build và sửa).

=> VIỆC TIẾP THEO NGAY: hoàn tất B1->B3, rồi lint + build + rebuild
   frontend container + smoke test lại board view.

--------------------------------------------------------------------------------
B+. ĐÃ LÀM TIẾP (vòng refactor UI thứ 3 — floating window + custom tags)
--------------------------------------------------------------------------------

B4. TASK DETAIL -> RESIZABLE & DRAGGABLE FLOATING WINDOW (react-rnd)
    - Cài react-rnd@10.5.3 (tương thích React 19, không cần legacy-peer-deps).
    - TaskModal.tsx viết lại hoàn toàn: KHÔNG còn dùng Modal.tsx chung nữa.
      Tự render backdrop fixed inset-0 z-50 bg-black/40 backdrop-blur-sm +
      <Rnd bounds="window" dragHandleClassName="drag-handle">:
        * default 850x550, căn giữa bằng offset tính từ window.innerWidth/Height
          (clamp min 16px); minWidth/minHeight 500/400; maxWidth/maxHeight 1080/600.
        * Title bar .drag-handle cursor-move select-none bg-gray-50 border-b
          px-6 py-4: icon Sparkles + "Task Details" + nút X đóng. Kéo được cả
          cửa sổ, resize 8 hướng theo Rnd mặc định.
        * Body: form grid grid-cols-1 md:grid-cols-3 gap-6 overflow-y-auto p-6.
          Trái md:col-span-2: title input raw style text-xl font-bold
          border-transparent focus:border-blue-500, textarea min-h-[150px]
          resize-y focus:ring-2 focus:ring-blue-500, Assigned Members + popover.
          Phải md:col-span-1 md:border-l md:pl-6 space-y-4: Priority select,
          Due date, Tags (pill màu + x tháo nhanh), Save Changes (disabled khi
          !isDirty) + Delete Task (danger). Đã bỏ nút Cancel (X là đủ).
      - Escape đóng cửa sổ (useEffect keydown); KHÔNG đóng khi click backdrop
        (tránh close ngoài ý muốn khi đang kéo).
    - Lint PASS, build PASS (index-D4ylW17S.js), 16/16 vitest PASS,
      docker compose up -d --build frontend OK, FE serve đúng bundle mới.

B5. TAG PICKER POPOVER MỚI — CUSTOM TAGS WITH COLOR PICKER
    - Component mới frontend/src/features/task/TagPickerPopover.tsx:
        * Top form: input "Create new tag name..." (Enter hoặc nút Create Tag),
          palette 6 chấm màu (#ef4444/#10b981/#3b82f6/#f97316/#8b5cf6/#eab308),
          validate tên rỗng -> disable nút.
        * Gọi tagApi.create = POST /api/workspaces/{workspaceId}/tags, invalidate
          ["workspace-tags"], onCreated(tag) để TaskModal tự gắn tag mới vào task.
        * Danh sách tag workspace: dot màu + tên + Check khi đã gắn, click toggle.
    - TaskModal tích hợp: taskTags local state; addTag/removeTag mutation tức thì;
      handleTagCreated chèn optimistic vào taskTags trước khi refetch; pill tag
      inline-flex gap-1.5 px-2.5 py-1 rounded-full text-xs font-semibold text-white
      nền động theo tag.color, nút x gỡ nhanh.

B6. CỘT KANBAN GIỚI HẠN ~3 CARDS + SCROLLBAR TÙY BIẾN (KanbanColumn.tsx)
    - Container cards: max-h-[400px] (~3 cards + gaps), overflow-y-auto,
      gap-3 px-1 py-1; bỏ scrollbar-thin, thay bằng Tailwind arbitrary variants:
      [&::-webkit-scrollbar]:w-1.5 / track:bg-transparent / thumb:bg-gray-300
      rounded-full / hover:thumb:bg-gray-400.
    - Header thêm mb-1 border-b, footer thêm mt-2 border-t pt-1 (header/footer
      nằm NGOÀI vùng cuộn nên luôn đứng yên — không cần sticky).

B7. BACKEND BỔ SUNG DELETE /api/tags/{tagId} (trước đó thiếu hoàn toàn)
    - TagService.deleteTag(tagId, callerId): tìm tag (404 nếu thiếu) ->
      check membership workspace của tag -> taskTagRepository.deleteAllByTagId
      (cascade thủ công join-table, đúng pattern plain-FK không associations)
      -> tagRepository.delete(tag) (Tag là BaseEntity nên hard-delete).
    - Thêm method port: TagRepository.delete(Tag),
      TaskTagRepository.deleteAllByTagId (+ impl/JPA deleteByTag_Id).
    - Backend tests 49/49 PASS; verify live: attach QA Smoke vào task ->
      GET task thấy [Feature, QA Smoke]; DELETE tag -> task về [Feature],
      workspace tags về 4. Endpoint DELETE trả ApiResponse<Void> như removeTag.

B8. FIX 2 LỖI LAYOUT BÁO CÁO BỞI USER (stacking context + flex-shrink)
    - Lỗi 1 — Task Modal bị chìm sau cards: ancestor của modal có transform
      (dnd Draggable) và backdrop-blur (header) tạo stacking context nên
      z-index trong modal không so được với cột bên cạnh.
      Fix: createPortal(..., document.body) trong TaskModal.tsx + backdrop
      fixed inset-0 z-[9999] pointer-events-auto. Modal giờ luôn ở tầng cao nhất.
    - Lỗi 2 — Cards bị co nén thay vì hiện scrollbar: container max-h khóa
      chiều cao, flex item mặc định shrink=1 => flexbox bóp nhỏ card.
      Fix: TaskCard outer thêm flex-shrink-0 min-h-[90px] (cả 2 Skeleton
      placeholder); container đổi p-1.5. Giờ card giữ nguyên kích thước,
      từ card thứ 4 trở đi mới cuộn.

B9. TINH CHỈNH KHU TAGS THEO SPEC MỚI (TaskModal.tsx + TagPickerPopover.tsx)
    - Pill tag: px-3 py-1 rounded-full text-xs font-semibold text-white
      shadow-sm, nền động theo tag.color (fallback #10b981), x tháo nhanh.
    - Nhãn "Tags" font-bold uppercase tracking-wider + "N applied".
    - Nút mở popover: "+ Add / edit tags" viền đứt border-2 border-gray-300,
      hover xanh blue, full-width mt-2.
    - Popover: absolute w-64 z-[100001] rounded-xl shadow-2xl p-3, layout 2
      khối — "Create New Tag" (input + palette 6 màu trên cùng hàng nút
      "+ Create", default green #10b981) và "Workspace Tags" (max-h-36 cuộn,
      dot màu + tên + ✓ khi đã gắn, click toggle).
    - Logic API giữ nguyên: tạo tag -> POST /workspaces/{id}/tags, tự gắn vào
      task qua onCreated; toggle tức thì addTag/removeTag; invalidate queries.
    - Verify: lint PASS, build PASS (index-DTyIE8un.js), 16/16 vitest PASS,
      container frontend rebuild xong, FE serve đúng bundle mới (HTTP 200).

GHI CHÚ SMOKE TEST (dễ dính lỡ):
- Login response field là data.accessToken (KHÔNG phải data.token) — script
  PowerShell lần đầu lấy sai field => 403 tưởng BE lỗi.
- Route tasks của list là GET /api/board-lists/{boardListId}/tasks (KHÔNG phải
  /api/lists/{id}/tasks) — đoán sai URL => 500 NoHandler.
- POST .../tags/{tagId} và DELETE tag trả ApiResponse<Void> (data=null) —
  đọc .data.tags sẽ ra rỗng dù thành công; phải GET lại task để xác minh.
- 409 Conflict khi tạo tag trùng tên trong workspace (DuplicateResourceException)
  — nhớ dọn tag test ("QA Smoke") sau mỗi lần smoke.

--------------------------------------------------------------------------------
B++. TRẠNG THÁI HIỆN TẠI CỦA STACK & MÃ NGUỒN (snapshot cuối session)
--------------------------------------------------------------------------------

- Containers: backend healthy · frontend healthy · mysql healthy · redis up.
- Frontend bundle đang serve: index-DTyIE8un.js (chứa TOÀN BỘ vòng A5->B9).
- Backend image chứa endpoint DELETE /api/tags/{tagId} mới (B7).
- Dependency mới thêm vào package.json: react-rnd@10.5.3 (đã cập nhật lockfile).
- Các file frontend bị thay đổi trong các vòng B4->B9:
    TaskModal.tsx (viết lại hoàn toàn: Rnd floating window + createPortal),
    TagPickerPopover.tsx (MỚI), KanbanColumn.tsx, TaskCard.tsx,
    Modal.tsx (size "2xl" — hiện không còn được TaskModal dùng).
- Các file backend bị thay đổi: TagController.java, TagService.java,
    TagRepository.java, TagRepositoryImpl.java, TaskTagRepository.java,
    TaskTagRepositoryImpl.java, TaskTagJpaRepository.java.
- Trạng thái git: TOÀN BỘ chưa commit (chờ user duyệt — xem C1).

--------------------------------------------------------------------------------
C. SẼ LÀM (backlog, theo thứ tự ưu tiên)
--------------------------------------------------------------------------------

C1. COMMIT + PUSH TOÀN BỘ (quan trọng nhất, chờ user duyệt)
    - Working tree đang chứa ~100+ file chưa commit: refactor bỏ tầng
      Project, My Tasks, các fix bug A2/A3/A5/B*, seeder A4, vòng B4->B9
      (floating window, custom tags, giới hạn 3 cards/cột, portal fix,
      flex-shrink fix, DELETE tag endpoint).
    - Sau push, CI GitHub sẽ chạy integration tests (Testcontainers) trên
      ubuntu runner — theo dõi tab Actions, đặc biệt job "Backend
      (build + test)".

C2. THEO DÕI SAU PUSH
    - Frontend CI (lint+test+build), Docker publish (2 job, tag lowercase
      đã fix từ session trước) — kỳ vọng GREEN toàn bộ.

C3. BACKEND TEST BỔ SUNG (khuyến nghị)
    - Thêm 1 test cho hành vi vừa fix: soft-deleted list KHÔNG xuất hiện
      lại trong GET /boards/{id}/lists (tránh regression @SQLRestriction).

C4. BACKLOG TỪ REFACTOR-NOTES (section 8) — chưa hẹn ngày
    - Real-time notifications (WebSocket/SSE) thay polling 60s
    - Notification khi được assign task
    - Board reordering, transfer ownership, file attachments, comments

--------------------------------------------------------------------------------
D. GHI CHÚ MÔI TRƯỜNG / KỸ THUẬT CẦN NHỚ
--------------------------------------------------------------------------------

- Docker Desktop path lạ: C:\Users\pc\AppData\Local\Programs\DockerDesktop\
  (KHÔNG ở Program Files). Engine 29.7.2.
- MySQL container: root password change_me_root, DB mini_trello
  (compose đọc từ env default ${DB_*:-...}).
- Testcontainers vẫn KHÔNG chạy được local (bug npipe dockerDesktopLinuxEngine
  HTTP 400) — integration tests chỉ chạy được trên CI ubuntu runner.
- PowerShell 5.1: không dùng ?? operator; chuỗi cmd bằng ; hoặc if ($?).
- Stack ports: FE http://localhost :80 · BE http://localhost:8080 ·
  Swagger /swagger-ui/index.html.
- Tài khoản demo: pm@test.com / member@test.com — mật khẩu DemoPass1.
- Frontend bundle mới: nhớ hard-refresh Ctrl+Shift+R khi test UI.

(End of file)
